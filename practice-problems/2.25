float sum_elements(float a[], unsigned length) {
  float result = 0;
  for (int i = 0; i <= length - 1; i++) {
    result += a[i];
  }
  return result;
}

variable length has type unsigned which implicitly cast to int

solution:

* make `length` int
* i < length
