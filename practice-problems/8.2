int main() {
  int x = 1;

  if (Fork() == 0) {
    printf("p1: x=%d\n", ++x);
  }
  printf("p2: x=%d\n", --x);
  exit(0);
}

A. What is the output of the child process?
-------------------------------------------
p1: x=2
p2: x=1

B. What is the output of the parent process?
--------------------------------------------
p2: x=0
